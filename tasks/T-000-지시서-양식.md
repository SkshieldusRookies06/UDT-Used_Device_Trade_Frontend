# T-000 — 지시서 양식 (복사해서 쓴다)

> 파일명은 `T-<번호>-<한줄요약>.md`. 번호는 **백엔드 리포의 티켓 후보표와 공유**한다
> (`../UDT-Used_Device_Trade-backend/tasks/T-000-지시서-양식.md`). 같은 번호가 두 리포에 있으면 그건
> 계약 개정 짝이다 (SPEC.md §12).

**담당:** (이름 · FE-A / FE-B / FE-C)
**브랜치:** `fe-<작업명>`

## [배경]
`../UDT-Used_Device_Trade-backend/SPEC.md` §N 본문을 **그대로 붙여넣는다.** 링크만 주지 않는다 —
에이전트에 넣을 때 이 파일 하나로 컨텍스트가 완결되어야 한다.

## [목표]
무엇을 만들면 되는지 한 문장.

## [수용 기준]
- `npm run build` 에러 0
- `seams/smoke.md` N번 줄 통과
- (API 소비면) `npm run gate` green

## [제약]
- 수정 허용 파일: (경로 목록)
- `seams/` 수정 금지 · 백엔드 리포 수정 금지 · 타인 `pages/` 폴더 수정 금지
- 전면 재작성 금지 (최소 diff)
- commit·push는 사람이

## [확인 명령]
```
npm run build && npm run gate
```
